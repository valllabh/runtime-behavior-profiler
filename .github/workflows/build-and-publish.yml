name: Build and Publish

on:
  push:
    tags:
    - 'v*.*.*'
  pull_request:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.17'

    - name: Build binary
      run: make build-with-version VERSION=${{ github.ref_name }}

    - name: Archive binary
      uses: actions/upload-artifact@v3
      with:
        name: tetragon-event-listener-${{ github.ref_name }}
        path: bin/tetragon-event-listener

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Download binary
      uses: actions/download-artifact@v3
      with:
        name: tetragon-event-listener-${{ github.ref_name }}
        path: bin/

    - name: Create GitHub Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref_name }}
        release_name: Release ${{ github.ref_name }}
        draft: false
        prerelease: false

    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: bin/tetragon-event-listener
        asset_name: tetragon-event-listener-${{ github.ref_name }}
        asset_content_type: application/octet-stream
